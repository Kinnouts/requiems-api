<%
  # Reusable button component with variants
  # Usage:
  #   <%= render "shared/button", text: "Click me", variant: "primary" %>
  #   <%= render "shared/button", text: "Delete", variant: "danger", size: "sm" %>
  #   <%= render "shared/button", text: "Learn More", variant: "outline", href: "/docs" %>

  variant ||= "primary"
  size ||= "md"
  type ||= "button"
  href ||= nil
  class_names ||= ""
  disabled ||= false
  data ||= {}

  # Base styles
  base = "inline-flex items-center justify-center font-medium rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed"

  # Size variants
  sizes = {
    "xs" => "px-2.5 py-1.5 text-xs",
    "sm" => "px-3 py-2 text-sm",
    "md" => "px-4 py-2 text-sm",
    "lg" => "px-5 py-3 text-base",
    "xl" => "px-6 py-3.5 text-base"
  }

  # Color variants
  variants = {
    "primary" => "bg-blue-600 text-white hover:bg-blue-700 focus:ring-blue-500",
    "secondary" => "bg-gray-600 text-white hover:bg-gray-700 focus:ring-gray-500",
    "success" => "bg-green-600 text-white hover:bg-green-700 focus:ring-green-500",
    "danger" => "bg-red-600 text-white hover:bg-red-700 focus:ring-red-500",
    "warning" => "bg-yellow-600 text-white hover:bg-yellow-700 focus:ring-yellow-500",
    "outline" => "bg-white border-2 border-gray-300 text-gray-700 hover:bg-gray-50 focus:ring-gray-500",
    "outline-primary" => "bg-white border-2 border-blue-600 text-blue-600 hover:bg-blue-50 focus:ring-blue-500",
    "ghost" => "bg-transparent text-gray-700 hover:bg-gray-100 focus:ring-gray-500",
    "link" => "bg-transparent text-blue-600 hover:underline focus:ring-blue-500"
  }

  classes = [base, sizes[size], variants[variant], class_names].compact.join(" ")
%>

<% if href.present? %>
  <%= link_to href, class: classes, data: data do %>
    <%= text %>
  <% end %>
<% else %>
  <button type="<%= type %>" class="<%= classes %>" <%= "disabled" if disabled %> <%= data.map { |k, v| "data-#{k}=\"#{v}\"" }.join(" ").html_safe %>>
    <%= text %>
  </button>
<% end %>
