<% content_for :title, "API Keys" %>

<!-- Header -->
<div class="mb-8">
  <h1 class="text-3xl font-bold text-gray-900">API Keys</h1>
  <p class="mt-2 text-sm text-gray-600">View and manage all API keys across users</p>
</div>

<!-- Search and Filters -->
<div class="bg-white rounded-lg shadow p-6 mb-8">
  <%= form_with url: admin_api_keys_path, method: :get, local: true, class: "space-y-4" do |f| %>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
      <div class="md:col-span-2">
        <%= f.label :search, "Search", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.text_field :search,
          value: params[:search],
          placeholder: "Search by key prefix, user email, or name",
          class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" %>
      </div>

      <div>
        <%= f.label :active, "Status", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.select :active,
          options_for_select([["All", ""], ["Active", "true"], ["Revoked", "false"]], params[:active]),
          {},
          class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" %>
      </div>
    </div>

    <div class="flex items-center space-x-2">
      <%= f.submit "Apply Filters", class: "px-6 py-2 bg-blue-600 text-white font-medium rounded-lg hover:bg-blue-700 transition-colors" %>
      <%= link_to "Clear", admin_api_keys_path, class: "px-6 py-2 border border-gray-300 text-gray-700 font-medium rounded-lg hover:bg-gray-50 transition-colors" %>
    </div>
  <% end %>
</div>

<!-- API Keys Table -->
<div class="bg-white rounded-lg shadow">
  <div class="px-6 py-4 border-b border-gray-200">
    <h2 class="text-lg font-semibold text-gray-900">
      API Keys (<%= @pagy.count %>)
    </h2>
  </div>

  <% if @api_keys.any? %>
    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Key</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">User</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Environment</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Created</th>
            <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% @api_keys.each do |key| %>
            <tr class="hover:bg-gray-50">
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="text-sm font-medium text-gray-900"><%= key.name %></div>
                <div class="text-xs text-gray-500 font-mono"><%= key.masked_key %></div>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600">
                <%= link_to key.user.email, admin_user_path(key.user), class: "text-blue-600 hover:text-blue-700" %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap">
                <%= render "partials/shared/badge",
                  text: h(key.environment.to_s.titleize.presence || "Live"),
                  variant: key.environment == "live" ? "primary" : "default",
                  size: "xs" %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap">
                <% if key.revoked_at.present? %>
                  <%= render "partials/shared/badge", text: "Revoked", variant: "danger", size: "xs" %>
                <% else %>
                  <%= render "partials/shared/badge", text: "Active", variant: "success", size: "xs" %>
                <% end %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600">
                <%= key.created_at.strftime("%b %d, %Y") %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                <%= link_to "View", admin_api_key_path(key), class: "text-blue-600 hover:text-blue-700" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <!-- Pagination -->
    <div class="px-6 py-4 border-t border-gray-200">
      <%== pagy_nav(@pagy) if @pagy.pages > 1 %>
    </div>
  <% else %>
    <div class="px-6 py-12 text-center">
      <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z"/>
      </svg>
      <h3 class="mt-2 text-sm font-medium text-gray-900">No API keys found</h3>
      <p class="mt-1 text-sm text-gray-500">Try adjusting your search or filters.</p>
    </div>
  <% end %>
</div>
