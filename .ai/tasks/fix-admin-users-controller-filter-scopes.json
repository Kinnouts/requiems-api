{
  "id": "fix-admin-users-controller-filter-scopes",
  "title": "Extract User search/filter/sort conditions to model scopes",
  "description": "admin/users_controller#index is 54 lines mixing search, plan filter, status filter, and sort logic inline. Extract each filter condition to a named scope on User (e.g. User.search_by(query), User.with_plan(plan), User.with_status(status)) and compose them in the controller.",
  "pain_score": 6,
  "files": [
    "apps/dashboard/app/controllers/admin/users_controller.rb",
    "apps/dashboard/app/models/user.rb"
  ]
}
