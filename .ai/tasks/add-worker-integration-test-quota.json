{
  "id": "add-worker-integration-test-quota",
  "title": "Add integration tests for quota-exceeded path in auth gateway",
  "description": "auth-gateway tests likely don't cover the monthly quota exceeded path end-to-end (KV miss → D1 query → quota check → 429). Add a vitest integration test that seeds D1 with an over-limit user and verifies the gateway returns 429 with correct headers.",
  "pain_score": 5,
  "files": [
    "apps/workers/auth-gateway/src/requests.ts",
    "apps/workers/auth-gateway/src/index.ts"
  ]
}
