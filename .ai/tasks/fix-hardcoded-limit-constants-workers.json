{
  "id": "fix-hardcoded-limit-constants-workers",
  "title": "Extract magic number limits in api-management routes to named constants",
  "description": "usage/export.ts uses limit.max(5000) and analytics/by-endpoint.ts uses limit.max(100) as raw magic numbers. Extract to named constants (USAGE_EXPORT_MAX_LIMIT, ANALYTICS_ENDPOINT_MAX_LIMIT) in shared config so they are self-documenting and easy to change.",
  "pain_score": 5,
  "files": [
    "apps/workers/api-management/src/routes/usage/export.ts",
    "apps/workers/api-management/src/routes/analytics/by-endpoint.ts",
    "apps/workers/shared/src/config.ts"
  ]
}
