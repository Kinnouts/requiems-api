{
  "id": "fix-missing-index-api-keys-search",
  "title": "Add index to support api_keys.key_prefix search",
  "description": "Admin searches api_keys by key_prefix using ILIKE. Without an index this is a sequential scan. Add a text search index (e.g. using pg_trgm GIN index or a btree index on key_prefix if exact-prefix search is sufficient).",
  "pain_score": 6,
  "files": [
    "apps/dashboard/db/schema.rb",
    "apps/dashboard/db/migrate"
  ]
}
