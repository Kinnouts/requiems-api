{
  "id": "fix-env-validation-pattern-duplicated-workers",
  "title": "Extract duplicated env validation pattern to shared createEnvValidator utility",
  "description": "Both auth-gateway and api-management define nearly identical envSchema + validateEnv() functions (define zod schema, call .parse(), export ValidatedEnv type). Extract a createEnvValidator<T>(schema) factory function to apps/workers/shared/src/env.ts so both workers use the same utility.",
  "pain_score": 5,
  "files": [
    "apps/workers/auth-gateway/src/env.ts",
    "apps/workers/api-management/src/env.ts",
    "apps/workers/shared/src/env.ts"
  ]
}
