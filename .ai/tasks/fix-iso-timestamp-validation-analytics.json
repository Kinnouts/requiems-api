{
  "id": "fix-iso-timestamp-validation-analytics",
  "title": "Validate ISO 8601 format for since/until params in analytics routes",
  "description": "The since and until query parameters in analytics/by-date.ts only validate .min(1), accepting any string. Invalid values are passed directly into D1 date comparisons, producing wrong results silently. Change to z.string().datetime() for strict ISO 8601 validation.",
  "pain_score": 6,
  "files": [
    "apps/workers/api-management/src/routes/analytics/by-date.ts"
  ]
}
